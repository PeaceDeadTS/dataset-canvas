NODE_ENV=production
LISTEN_SOCKET=/tmp/dataset-canvas.sock
# Used only when LISTEN_SOCKET is empty (local `npm run dev`)
PORT=5000
CORS_ORIGIN=https://data.pbc.red
JWT_SECRET=

DB_USER=
DB_PASSWORD=
DB_DATABASE=dataset_canvas
DB_SOCKET_PATH=/var/run/mysqld/mysqld.sock
# DB_HOST=localhost
# DB_PORT=3306

UPLOADS_DIR=
PUBLIC_MEDIA_BASE=https://cdn2.pbc.wiki
